import validate from './validate';
import until from './library/until';

// const base64Library = { toUint8Array: until.base64ToUint8Array };
// const arrayBufferLibrary = { toBase64: until.byteArrayToBase64 };
// const validate = { cardNo, fixedPhone, idCard, phone };

// export { until };

// export default { arrayToTree, base64Library, arrayBufferLibrary, validate };

export default {
  until,
  validate
};
