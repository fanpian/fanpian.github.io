import regExps from './RegExpCollection';
import arrayToTree from './arrayToTree';
import {
  getParams,
  getParamByName,
  base64ToUint8Array,
  byteArrayToBase64,
  validate
} from './tool';

const utils = {
  regExps,
  arrayToTree,
  getParams,
  getParamByName,
  base64ToUint8Array,
  byteArrayToBase64,
  validate
};

export default utils;
